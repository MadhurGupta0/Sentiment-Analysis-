<!DOCTYPE html>
<html lang="en">
<head>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sentiment Analysis</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h2 class="text-center">Customer Review Sentiment Analysis</h2>
            <div class="mb-3">
                <label for="fileInput" class="form-label">Upload CSV or XLSX file:</label>
                <input class="form-control" type="file" id="fileInput" accept=".csv, .xlsx" required>
            </div>
            <button id="analyze" class="btn btn-primary">Analyze</button>
            {% if not a %}
            <button id="startBackendButton">Start Backend Server</button>
        {% endif %}

         <div id="loading" style="display:none;">Processing...</div>

        <div class="mt-4">
            <h4>Sentiment Results:</h4>
            <ul>
                <li><strong>Positive:</strong><span id="positive"></span></li>
                <li><strong>Negative:</strong><span id="negative"></span></li>
                <li><strong>Neutral:</strong><span id="neutral"></span></li>
            </ul>
        </div>
    </div>

    <script src="{{ url_for('..static', filename='js/app.js') }}"></script>
</body>
</html>
